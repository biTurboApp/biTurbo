[
  {"kind":"bug","severity":"high","title":"Corrupt/missing uidmap silently resets id map while stale vectors stay loaded","file":"src-tauri/src/index_engine.rs","lines":"48-61","description":"open_or_create parses {project_id}.uidmap.json with serde_json::from_slice(&bytes).unwrap_or_default(); any parse error yields an EMPTY map and next_extid=1 while the loaded .tvim still contains all old vectors under extids 1..N. Same empty-map state whenever meta file missing but tvim exists (e.g. crash between the two renames in persist_now). First subsequent add()/add_batch() assigns extid 1.. into an index that already contains those ids, so uid->vector attribution silently diverges: searches return uids whose stored vector belongs to another memory. len() also reports 0 for a non-empty index, defeating count-based repair checks.","evidence":"let map: HashMap<String, u64> = serde_json::from_slice(&bytes).unwrap_or_default();","suggestion":"Treat unreadable/inconsistent meta as fatal or trigger full rebuild from SQLite; never fall back to empty map over non-empty index."},
  {"kind":"bug","severity":"high","title":"Index persist uses two renames without fsync; crash leaves tvim/uidmap mismatched","file":"src-tauri/src/index_engine.rs","lines":"180-203","description":"persist_now writes tmp files, renames tmp_index then tmp_meta, never fsyncs either file or directory. Failure modes: (1) crash between the two renames leaves NEW .tvim paired with OLD .uidmap.json — on reload next_extid collides with extids already present; (2) without fsync-before-rename, APFS/ext4 may persist rename without file data, producing zero-length or garbage targets despite the 'crash mid-write never corrupts' comment.","evidence":"drop(inner);\nstd::fs::rename(&tmp_index, &self.file_path)?;\nstd::fs::rename(&tmp_meta, &meta)?;\nself.dirty.store(false, Ordering::Release);","suggestion":"Fsync both tmp files before renaming and fsync parent directory after; store generation counter in both files so mismatched pair detected on load and repaired."},
  {"kind":"bug","severity":"high","title":"assert_eq! panics on dimension mismatch instead of returning an error","file":"src-tauri/src/index_engine.rs","lines":"89,109,211,249","description":"add(), add_batch(), search(), search_filtered() all assert_eq!(vector.len(), self.dim). self.dim comes from projects.dim while vectors come from embedder_for_project honoring per-project embed_model. During/after a model switch to different-dimension model, or when cached ProjectIndex predates a dim change, embed returns different length and the assert panics the calling thread — killing the MCP stdio server loop or a Tauri command — instead of returning BiError. Note: release profile sets panic=abort, so this kills the entire process.","evidence":"pub fn add(&self, uid: &str, vector: &[f32]) -> BiResult<()> {\n    assert_eq!(vector.len(), self.dim, \"vector dim mismatch\");","suggestion":"Replace asserts with BiError::Index returns (validate once in open_or_create against loaded IdMapIndex dims); surface a 'rebuild required' error."},
  {"kind":"bug","severity":"medium","title":"Dirty flag cleared after snapshot write can lose a concurrent mutation from disk","file":"src-tauri/src/index_engine.rs","lines":"195-201","description":"persist_now serializes under inner lock, drops it, renames, stores dirty=false. A concurrent add()/add_batch() can commit and mark_dirty() in the window between serialization and dirty.store(false); the store erases the new mutation's dirty bit. Just-added vector never persisted; if process exits before any later mutation, SQLite has the memory but index file does not, requiring expensive re-embedding via repair.","evidence":"self.dirty.store(false, Ordering::Release);","suggestion":"Capture mutation generation counter under inner lock during serialization; only clear dirty if counter unchanged."},
  {"kind":"bug","severity":"medium","title":"add_batch commits uid maps before the fallible add_with_ids; errors leave phantom mappings","file":"src-tauri/src/index_engine.rs","lines":"118-138","description":"In add_batch, uid_to_extid/extid_to_uid are inserted and removed-entries dropped BEFORE inner.index.add_with_ids(...) runs. If add_with_ids fails the function returns Err but maps already claim every uid exists — including uids whose previous vectors were removed and never re-added. Subsequent searches filter through extid_to_uid and contains_uid()/len() report entries the vector index does not have. add_one() correctly inserts maps only after add_with_ids succeeds; batch path diverges.","evidence":"inner.uid_to_extid.insert(uid.clone(), extid);\ninner.extid_to_uid.insert(extid, uid.clone());\n...\ninner.index.add_with_ids(&flat, &ids).map_err(...)?;","suggestion":"Stage map deltas in locals and apply to inner only after add_with_ids succeeds, mirroring add_one's ordering."},
  {"kind":"bug","severity":"medium","title":"Coalesced manual consolidate requests are dropped; queued flag never becomes a job","file":"src-tauri/src/scheduler.rs","lines":"102-107,120-124","description":"When enqueue() finds g.running it sets g.queued=true and returns WITHOUT sending on the channel; the Full branch of try_send does the same. Nothing ever reads g.queued to synthesize a follow-up job: worker only clears the flag when it dequeues its next channel message, and run_once ignores it. A user clicking 'consolidate now' while a run is in flight loses the request entirely, and get_status() reports queued=true / next_run_in_secs=0 indefinitely. io.rs implements the correct re-run pattern; scheduler.rs does not.","evidence":"if g.running {\n    // Coalesce: one manual job is enough while another runs. Mark queued.\n    g.queued = true;\n    return Ok(());\n}","suggestion":"Always try_send (capacity 8 buffers fine), or have worker after each run_once re-check g.queued and enqueue a ManualJob before blocking on recv()."},
  {"kind":"bug","severity":"medium","title":"Migration connection lacks busy_timeout and ignores cross-process write lock","file":"src-tauri/src/db.rs","lines":"60-79","description":"prepare_database serializes migrations across processes via biturbo.migrate.lock, but the raw rusqlite::Connection::open used for migration has no busy_timeout (SQLite default 0) and does not take the biturbo.write.lock that Db::write users hold. If GUI migrates while MCP sidecar is mid-write, migration transaction gets immediate SQLITE_BUSY, run_migrations fails, startup aborts after pointless backup+restore cycle.","evidence":"let mut conn = rusqlite::Connection::open(db_path)?;\nlet version: i64 = conn.query_row(\"PRAGMA user_version\", [], |r| r.get(0))?;","suggestion":"Execute PRAGMA busy_timeout=5000 on migration connection and acquire same write.lock for migration duration."},
  {"kind":"bug","severity":"medium","title":"Unsanitized project id flows into index/lock filenames enabling path traversal","file":"src-tauri/src/persistence.rs","lines":"71-74","description":"SECURITY: project::create accepts CreateProjectInput.id verbatim (only fallback goes through slugify), and resolve_project_id echoes MCP-supplied ids. That id is interpolated into filesystem paths: format!(\"{project_id}.mutation.lock\") here, format!(\"{project_id}.tvim\")/.uidmap.json in index_engine.rs and state.rs. An id like '../../LaunchAgents/evil' makes the app create/write files outside the data directory (and delete them in project::delete).","evidence":"let lock_path = self\n    .data_dir\n    .join(\"indices\")\n    .join(format!(\"{project_id}.mutation.lock\"));","suggestion":"Validate project ids against ^[A-Za-z0-9_-]+$ in project::create; reject separators in resolve_project_id consumers; or percent-encode ids in filenames."},
  {"kind":"improvement","severity":"medium","title":"Applied journal rows, activity log, and recall events grow without bound","file":"src-tauri/src/persistence.rs","lines":"149-153","description":"replay_index_mutations only sets applied_at; applied index_mutations rows (which embed full memory content) never deleted, and nothing prunes activity (log_activity fires on every read/write/search) or recall_events/recall_feedback. For long-lived projects with heavy agent traffic these tables grow forever, bloating DB file, backups (each migration copies whole DB), and slowing scans. No DELETE FROM activity/index_mutations/recall_events exists anywhere.","evidence":"\"UPDATE index_mutations SET applied_at = ?1\n WHERE project_id = ?2 AND applied_at IS NULL AND id <= ?3\",","suggestion":"Add retention pruning in scheduler tick: delete applied index_mutations older than N days, cap activity/recall_events per project or by age."},
  {"kind":"improvement","severity":"medium","title":"Blocking consolidate runs directly on the tokio async runtime threads","file":"src-tauri/src/scheduler.rs","lines":"51-57,80-88,197","description":"run_once is an async fn that calls consolidate::consolidate(state, project_id) synchronously; consolidate does heavy CPU + synchronous SQLite/embedding work that can run minutes. Both periodic loop task and manual worker task park a tauri async_runtime worker thread for the whole run, starving other tasks (UI events, other commands).","evidence":"let result = consolidate::consolidate(state, project_id);","suggestion":"Wrap the synchronous consolidate call in tokio::task::spawn_blocking or dedicated thread + handle."},
  {"kind":"bug","severity":"low","title":"run_now_blocking bypasses the running gate and can race the consolidate worker","file":"src-tauri/src/scheduler.rs","lines":"135-157","description":"run_now_blocking calls consolidate::consolidate directly without checking or setting Shared::running, so any caller can start a second concurrent consolidate while periodic loop or manual worker is mid-run — exactly what the module comment says must never happen. Also sets g.running=false on completion, clobbering the flag if another run is active.","evidence":"pub fn run_now_blocking(state: &AppState) -> BiResult<ConsolidateReport> {\n    let report = consolidate::consolidate(state, None)?;","suggestion":"Route through same gate (check-and-set g.running, return Busy if held) or delete the compatibility alias."},
  {"kind":"improvement","severity":"low","title":"Consolidate failures only logged; frontend never notified","file":"src-tauri/src/scheduler.rs","lines":"242-244","description":"On success run_once emits app.emit(\"consolidate:done\", &r); on error only tracing::warn!. UI keys off consolidate_status/consolidate:done, sees running flip false with stale last_report and no failure indication, so manual triggers appear to silently do nothing.","evidence":"Err(e) => {\n    tracing::warn!(\"consolidate run failed: {e}\");\n}","suggestion":"Emit a consolidate:error event (message + project_id) mirroring success path; record last_error in ConsolidateStatus."},
  {"kind":"improvement","severity":"low","title":"get_status reports a full 6h interval before the first startup run","file":"src-tauri/src/scheduler.rs","lines":"159-178","description":"Before any run completes, get_status returns next_run_in_secs = INTERVAL (21600), but periodic loop actually fires at STARTUP_DELAY (60s) after spawn. Status misreports next consolidation by ~5h58m during first minute after every fresh start.","evidence":"} else {\n    INTERVAL.as_secs()\n};","suggestion":"Track loop's next-due Instant (or subtract STARTUP_DELAY when last_finish is None)."},
  {"kind":"improvement","severity":"low","title":"content_hash persisted in journal but never verified during replay","file":"src-tauri/src/persistence.rs","lines":"42-54","description":"queue() computes SHA-256 of content into index_mutations.content_hash, but replay_index_mutations never reads it. Dead weight today and missed integrity check: comparing hash(content) at replay would detect journal/index drift cheaply before spending embedding compute, and let replay skip upserts whose content already matches.","evidence":"let content_hash = content.map(hash_text);","suggestion":"Verify content_hash against content in replay (error or force repair on mismatch)."},
  {"kind":"improvement","severity":"low","title":"persist_now holds the index mutex across full multi-MB serialize and file write","file":"src-tauri/src/index_engine.rs","lines":"180-197","description":"The whole flush — turbovec write() plus serde_json serialization of entire uid map — executes while holding inner's Mutex, so every search/add/remove on that project blocks on disk IO latency. For large indices this turns background flush ticks into visible recall-latency spikes; flusher runs maybe_flush every 5s.","evidence":"let inner = self.inner.lock();\n...\ninner.index.write(&tmp_index)...","suggestion":"Serialize to in-memory buffers while holding lock (or snapshot uid map clone), release lock, then file IO lock-free; clear dirty conditionally by generation."},
  {"kind":"bug","severity":"low","title":"Newer user_version (downgraded binary) silently accepted by prepare_database","file":"src-tauri/src/db.rs","lines":"76-79","description":"Guard is `if version >= CURRENT_SCHEMA_VERSION { return Ok(()) }`, so opening a database written by a NEWER biTurbo proceeds against unknown schema. Missing columns/tables then produce confusing runtime SQL errors instead of clear 'database created by newer version' message; migrations skipped entirely.","evidence":"if version >= CURRENT_SCHEMA_VERSION {\n    fs2::FileExt::unlock(&lock)?;\n    return Ok(());\n}","suggestion":"Reject (or explicitly warn and refuse) when version > CURRENT_SCHEMA_VERSION."},
  {"kind":"improvement","severity":"low","title":"Pre-migration database and index-metadata backups accumulate forever","file":"src-tauri/src/db.rs","lines":"82-95","description":"Every migration run copies full DB (conn.backup) plus all indices/*.json metadata into backups/, nothing ever deletes old backups. Copy happens whenever user_version < CURRENT_SCHEMA_VERSION (including repeated failed attempts which restore and retry on next launch), so a flapping migration loop creates unbounded series of full database copies.","evidence":"let path = backup_dir.join(format!(\"biturbo-pre-v{}-{}.db\", CURRENT_SCHEMA_VERSION, stamp));\nconn.backup(DatabaseName::Main, &path, None)?;","suggestion":"Retain only last N pre-migration backups (delete older after successful migration commit)."},
  {"kind":"bug","severity":"low","title":"replace_all publishes an empty index before repopulating and persists partial state on error","file":"src-tauri/src/index_engine.rs","lines":"291-305","description":"replace_all resets Inner to fresh empty index under lock, releases, re-acquires in add_batch. Between the two acquisitions concurrent searches observe empty index (recall returns nothing mid-rebuild); if add_batch fails partway the index left partially populated with old contents gone; mark_dirty already set so debounced flusher persists partial state to disk. Recovery depends on repair being triggered later by digest mismatch.","evidence":"*inner = Inner { index: IdMapIndex::new(self.dim, self.bit_width)..., next_extid: 1 };\n}\nself.mark_dirty();\nself.add_batch(items)?;","suggestion":"Build replacement Inner completely off to the side, then swap in under single lock; on error keep previous Inner intact."}
]
