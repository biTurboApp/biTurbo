[
  {"kind":"bug","severity":"high","title":"Deleting the current project leaves currentProjectId dangling, causing stale UI and orphan memory writes","file":"src/components/Sidebar.tsx","lines":"136-155","description":"deleteProject() never resets currentProjectId when the deleted project is the active one, and store.refreshProjects() cannot fix it because its fallback (`if (!get().currentProjectId && ...)`) is dead code — currentProjectId is initialized to \"default\" and never emptied. After deleting the active project: App.tsx's refetch effect (keyed on currentProjectId) doesn't re-run so Memories/tags show the deleted project's data, refreshGraph() fails and leaves the OLD project's graph rendered, and QuickAdd keeps submitting remember({project_id: <deleted-id>}) — backend memory::remember does no project-existence check, so memories are silently written with a dangling project_id that no view will ever list.","evidence":"await api.deleteProject(id);\nawait Promise.all([refreshProjects(), refreshStats(), refreshGraph().catch(() => {})]);","suggestion":"After a successful delete, if id === currentProjectId, set currentProjectId to the first remaining project (or \"default\") before refreshing; make refreshProjects() also validate that currentProjectId still exists."},
  {"kind":"bug","severity":"high","title":"Consolidate button gets stuck disabled forever when the consolidate run fails","file":"src/components/TopBar.tsx","lines":"27-52","description":"runConsolidate() sets consolidating=true and relies solely on the 'consolidate:done' Tauri event to reset it (the IPC call returns immediately after enqueueing). The backend scheduler's error branch only logs — no event emitted on failure. If the consolidate run errors, the button stays disabled with a pulsing icon indefinitely: no toast, no error, no way to retry except restarting the app.","evidence":"setConsolidating(true);\ntry {\n  await api.consolidate(currentProjectId); // returns immediately (enqueue)\n} catch (e) {\n  setConsolidating(false);\n  showToast({ kind: \"err\", text: String(e) });\n}","suggestion":"Have the backend emit 'consolidate:error' from the Err branch (and listen for it here), or poll consolidateStatus() with a timeout after enqueue."},
  {"kind":"bug","severity":"medium","title":"loadMoreMemories trim keeps the OLDEST 500 memories and deletes the newest page from the list","file":"src/lib/store.ts","lines":"159-175","description":"list_memories returns newest-first (ORDER BY created_at DESC), so combined = [...s.memories, ...batch] is ordered newest→oldest. The comment says 'keep the newest 500 memories' but combined.slice(-500) keeps the LAST 500 elements — i.e. the oldest ones — and discards the first page (the newest 50 memories). Trigger: scroll down through more than 500 memories; once the 11th page loads, the top of the list (most recent memories) vanishes and the scroll position jumps.","evidence":"const combined = [...s.memories, ...batch];\nconst trimmed = combined.length > 500 ? combined.slice(-500) : combined;","suggestion":"Use combined.slice(0, 500) for a newest-first list (keeping offset math consistent), or drop the trim and virtualize the list."},
  {"kind":"bug","severity":"medium","title":"api.consolidate mis-types its payload as ConsolidateReport but the backend returns ConsolidateStatus","file":"src/lib/api.ts","lines":"156-159","description":"The invoke wrapper declares invoke<ConsolidateReport>(\"consolidate_now\"), but commands.rs consolidate_now returns BiResult<ConsolidateStatus>. Any consumer reading .decayed/.merged/.duplicates_found off the resolved value gets undefined. Today TopBar ignores the result so this is latent, but the wrapper is the typed contract for future callers.","evidence":"consolidate: (project_id?: string | null) =>\n  invoke<ConsolidateReport>(\"consolidate_now\", { projectId: project_id ?? null }),","suggestion":"Type the wrapper as invoke<ConsolidateStatus> and read the report from .last_report when needed."},
  {"kind":"bug","severity":"medium","title":"store.startIngest/cancelIngest key ingestJobs by job_id while events key by project_id, orphaning entries","file":"src/lib/store.ts","lines":"209-232","description":"startIngest inserts the progress entry under [job.job_id], but the 'ingest:progress' listener upserts under p.project_id and 'ingest:done'/'ingest:error' delete under d.project_id. The job_id-keyed entry is never updated or removed: TopBar's activeIngests filter shows a 'queued…' spinner forever, and cancelIngest(job_id) removes the wrong key leaving the real progress entry stuck. The 'ingest:done' handler also builds {...s.ingestJobs[d.project_id], phase: \"done\"} which spreads undefined and casts an object missing required fields into IngestProgress when no progress entry exists yet.","evidence":"set((s) => ({ ingestJobs: { ...s.ingestJobs, [job.job_id]: { project_id, phase: \"queued\", ... } } }));\n// listeners: [p.project_id]: p   /   const { [d.project_id]: _, ...rest } = s.ingestJobs;","suggestion":"Key entries consistently by project_id everywhere (events already do), have cancelIngest take project_id, and remove the `as IngestProgress` cast."},
  {"kind":"bug","severity":"medium","title":"loadMoreMemories has no in-flight guard: duplicate pages and cross-project rows after switching projects","file":"src/lib/store.ts","lines":"159-176","description":"loadMoreMemories reads get().memoryOffset and get().currentProjectId before awaiting, then appends unconditionally after the await. Two rapid scroll triggers both read the same offset and append the same batch twice (duplicate uids). Worse, if the user switches projects while a page is in flight, the old project's batch is appended to the new project's freshly refreshed memories array — mixed-project list content with no visual indication.","evidence":"loadMoreMemories: async () => {\n  const offset = get().memoryOffset;\n  const batch = await api.listMemories({ project_id: get().currentProjectId, limit: 50, offset });\n  set((s) => { const combined = [...s.memories, ...batch]; ... });\n}","suggestion":"Capture projectId at call time and compare against get().currentProjectId before set(); add an isLoadingMore flag that makes concurrent calls no-op."},
  {"kind":"bug","severity":"medium","title":"Overview 12-week activity heatmap is built from at most 25 activity entries, rendering it mostly empty","file":"src/views/Overview.tsx","lines":"23-31","description":"The heatmap buckets 84 days (12*7) of activity, but the underlying activity array comes from bootstrap's recent_activity(state, 25) and refreshActivity(60) — and refreshActivity is only invoked after a manual consolidate. With heavy agent usage, 25 entries may cover only the last few hours, so the 'Activity · 12 weeks' card shows a handful of lit cells near today and 80+ permanently empty days regardless of real history.","evidence":"const days = 12 * 7;\nconst buckets = new Array(days).fill(0);\nfor (const a of activity) { // activity capped at 25 (bootstrap) / 60 (refresh)","suggestion":"Add a dedicated backend query aggregating per-day counts over 84 days (GROUP BY date) instead of paginating raw activity rows, or relabel the card."},
  {"kind":"bug","severity":"medium","title":"Overlapping confirm() calls: second dialog overwrites the resolver and the first promise never settles","file":"src/lib/store.ts","lines":"250-283","description":"confirm() stores its Promise resolver in a single module-local slot and replaces confirmState. If two callers await confirm() concurrently (bulk-forgetting in a loop, two components triggering deletes), the second call overwrites both the visible dialog and the resolver slot: caller #1's promise never resolves, so its awaiting code (including finally/cleanup) hangs forever.","evidence":"confirm: (opts) => {\n  set({ confirmState: opts });\n  return new Promise<boolean>((resolve) => { registerConfirmResolver(resolve); });\n},","suggestion":"Queue resolvers (FIFO) and only show the next dialog after the current resolves, or resolve previous promise(false) when superseded."},
  {"kind":"bug","severity":"medium","title":"MemoryCard is a click-only div: keyboard users cannot open memory details","file":"src/components/MemoryCard.tsx","lines":"44-48","description":"The card's entire interaction model is onClick/onContextMenu on a plain <div> — no tabIndex, no role, no onKeyDown. On the Overview grid and Memories list, keyboard-only users cannot select/open a memory detail. Feedback thumbs buttons expose only title tooltips (no aria-label).","evidence":"<div\n  onClick={onClick}\n  onContextMenu={handleContext}\n  className={clsx(\"memory-card\", active && \"active\")}>","suggestion":"Render as a button (or add role=\"button\", tabIndex={0}, onKeyDown Enter/Space), and give feedback buttons aria-labels."},
  {"kind":"bug","severity":"medium","title":"Modals lack focus traps: Tab escapes ConfirmModal despite aria-modal, QuickAdd has no dialog semantics","file":"src/components/ConfirmModal.tsx","lines":"91-101","description":"ConfirmModal declares role=\"dialog\" aria-modal=\"true\" but implements no focus trap — Tab cycles out of the modal into background controls visually blocked by the backdrop. QuickAdd's overlay has no role=\"dialog\", no aria-modal, and no Escape handler of its own beyond App's global one. Both modals leave screen-reader/keyboard users stranded.","evidence":"<div className=\"fixed inset-0 z-50 ...\" onMouseDown={...} role=\"dialog\" aria-modal=\"true\"> <!-- no Tab trap -->","suggestion":"Add a focus trap cycling Tab within the modal; add role=\"dialog\" aria-modal=\"true\" aria-label to QuickAdd's overlay."},
  {"kind":"bug","severity":"low","title":"Toast is never announced to assistive tech (missing role=status / aria-live)","file":"src/components/Toast.tsx","lines":"8-19","description":"Toasts carry operation results including errors ('Ingest failed: …'), but the container is a plain clickable div with no role=\"status\"/aria-live. Screen readers get no notification; the feedback channel is sight-only for destructive-operation outcomes.","evidence":"<div onClick={clear} className={clsx(\"fixed bottom-4 left-1/2 z-50 ...\", ...)}>","suggestion":"Add role=\"status\" aria-live=\"polite\" (role=\"alert\" for kind===\"err\") to the toast root."},
  {"kind":"bug","severity":"low","title":"Project-change effect fires refreshMemories without catch, causing unhandled promise rejections","file":"src/App.tsx","lines":"34-39","description":"refreshTags() and refreshGraph() are fire-and-forget with .catch(() => {}), but refreshMemories() is called bare. If list_memories rejects (e.g. right after deleting the active project), every project switch produces an unhandled promise rejection and the Memories view silently keeps stale data with no toast.","evidence":"refreshMemories();            // ← no .catch\nrefreshTags().catch(() => {});\nrefreshGraph().catch(() => {});","suggestion":"Mirror the siblings: refreshMemories().catch(() => {}) — or surface a toast so failures aren't fully swallowed."},
  {"kind":"bug","severity":"low","title":"MemoryDetail related-memories search can resolve out of order and render stale results","file":"src/components/MemoryDetail.tsx","lines":"30-49","description":"The related-items effect fires api.search per selected memory with no cancellation or staleness check. Clicking two related items quickly starts two searches; if the first resolves after the second, setRelated overwrites the newer memory's related list with results computed from the previous memory's content.","evidence":"(async () => {\n  const hits = await api.search({ project_id: memory.project_id, query: memory.content.slice(0, 200), k: 6 });\n  setRelated(hits.filter(...));\n})();","suggestion":"Add a local `cancelled` flag set in the effect cleanup and check it before setRelated."},
  {"kind":"bug","severity":"low","title":"QuickAdd shows an error toast after a successful save when the post-save refresh fails, inviting duplicate entries","file":"src/components/QuickAdd.tsx","lines":"40-50","description":"submit() closes the modal and clears the form, then awaits refreshMemories()/refreshStats() inside the same try block. If either refresh rejects after api.remember succeeded, control jumps to catch and shows an error toast — the user believes the memory was NOT saved and retypes it, creating a duplicate. The save already succeeded and the form was cleared, so the error is also mis-attributed.","evidence":"setContent(\"\"); setTags(\"\"); setOpen(false);\nawait refreshMemories();\nawait refreshStats();\nshowToast({ kind: \"ok\", text: \"Remembered\" });\n} catch (e) {\n  showToast({ kind: \"err\", text: String(e) }); // fires even though remember() succeeded","suggestion":"Show the success toast immediately after api.remember resolves; move refreshes outside the try."},
  {"kind":"bug","severity":"low","title":"tokenizeCode treats any unquoted '#...' to end-of-line as a comment, mis-highlighting real code","file":"src/lib/format.ts","lines":"130-134","description":"The comment alternative (\\/\\/.*$|#.*$) matches a '#' anywhere outside a string and swallows the rest of the line. In JS/TS code memories, private-field access like this.#count++ dims everything from '#' onward as a comment; unquoted hex colors (color: #fff) get the same treatment.","evidence":"const pattern = /(\\/\\/.*$|#.*$)|(\"(?:[^\"\\\\]|\\\\.)*\"|...)|(\\b\\d+(?:\\.\\d+)?\\b)|([A-Za-z_$][\\w$]*)|(\\s+)|(.)/g;","suggestion":"Restrict '#' comments to languages that use them (pass language flag from memory.language) or require '#' at line start/preceded by whitespace."},
  {"kind":"bug","severity":"low","title":"Heatmap weekday labels are misaligned: columns are rolling 7-day chunks, not calendar weeks","file":"src/components/Heatmap.tsx","lines":"6-24","description":"dayLabels [\"M\",\"\",\"W\",\"\",\"F\",\"\"] assumes row 0 of every column is Monday, but Overview fills values oldest-first as rolling day buckets ending TODAY. Column boundaries land on an arbitrary weekday; the M/W/F labels only line up when (today − 83 days) happens to be a Monday — wrong for 6 out of 7 possible todays.","evidence":"const dayLabels = [\"M\", \"\", \"W\", \"\", \"F\", \"\", \"\"];","suggestion":"Pad the front of the bucket array so column 0 starts on a Monday (like GitHub's contribution graph), or drop the letter labels."},
  {"kind":"bug","severity":"low","title":"truncatePath exceeds maxLen when the filename alone is longer than the limit","file":"src/lib/format.ts","lines":"77-90","description":"truncatePath only trims by dropping leading directories. When the final path segment is itself longer than maxLen (long generated/test filenames common in code indexes), the loop breaks immediately and returns '…/' + filename with no character-level cap — can return 60+ chars for maxLen 40, overflowing the code-chip layout.","evidence":"while (segments.length > 0) {\n  const next = segments.pop() + \"/\" + result;\n  if ((\"…/\" + next).length > maxLen) break;","suggestion":"After directory trimming, fall back to character truncation of the filename keeping the extension if possible."},
  {"kind":"improvement","severity":"medium","title":"Persist the active project across restarts instead of always resetting to the first indexed project","file":"src/lib/store.ts","lines":"235-242","description":"bootstrapOnce() always picks projects.find(p => p.indexed_count > 0)?.id ?? projects[0]?.id ?? \"default\", discarding whatever project the user was working in. Every app launch lands on an arbitrary project, forcing multi-project users to re-select context each session. The theme already demonstrates the localStorage pattern.","evidence":"const currentProjectId =\n  projects.find((p) => p.indexed_count > 0)?.id ??\n  projects[0]?.id ?? \"default\";","suggestion":"Store the last active project id in localStorage (validate it still exists before using, else fall back)."},
  {"kind":"improvement","severity":"low","title":"Sidebar footer presents total registered agents as a live 'connected' status","file":"src/components/Sidebar.tsx","lines":"33-34","description":"connectedAgents = agents.length counts every agent ever registered (table grows forever), displayed next to a green status dot implying live connectivity. Agents.tsx separately uses a 24h last_seen window for its 'live' badge, so the two surfaces disagree. The number goes stale: agents[] only refreshed on bootstrap and manual refresh.","evidence":"const connectedAgents = agents.length;\n<div className=\"font-medium text-text\">{connectedAgents} agent{...}</div>","suggestion":"Compute count with same recency window as Agents.tsx and label it 'seen recently', or drop the status-dot affordance."},
  {"kind":"improvement","severity":"low","title":"main.tsx reads localStorage unguarded at module scope and can crash the whole app at boot","file":"src/main.tsx","lines":"32-35","description":"The ZOOM_KEY write just above is wrapped in try/catch for quota/private-mode failures, but the read localStorage.getItem(ZOOM_KEY) is not. In environments where localStorage access throws (locked-down WebView settings), this top-level throw executes before ReactDOM.createRoot, leaving a blank window with no error surface.","evidence":"const raw = localStorage.getItem(ZOOM_KEY) ?? fromEnv;  // ← unguarded","suggestion":"Wrap the read in try/catch like the write, defaulting to fromEnv/undefined on failure."}
]
