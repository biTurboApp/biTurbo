[
  {"kind":"bug","severity":"high","title":"Graph layout request silently dropped on first mount (worker ref null)","file":"src/views/Graph.tsx","lines":"96-124,151-153,685-687","description":"The layout effect (line 96) is declared before the worker-attach effect (line 151), and React runs mount effects in declaration order. layoutWorkerRef is a module-level ref initialized to null, so on the first-ever mount of <Graph> in a session the effect at line 109 calls layoutWorkerRef.current?.postMessage(...) while the ref is still null. Optional chaining silently swallows the request; the worker effect then creates the worker but nothing re-posts the layout. Result: the first visit to the Graph tab per app launch shows only the jittered seed-circle layout and never refines via Barnes-Hut, until the user toggles a kind filter or the graph refreshes.","evidence":"const reqId = ++layoutReqSeq.current;\n    layoutWorkerRef.current?.postMessage({ type: \"layout\", requestId: reqId, ... });\n// later, effect declared at line 151:\n    const w = ensureLayoutWorker();\n    layoutWorkerRef.current = w;","suggestion":"Create the worker before posting: replace layoutWorkerRef.current?.postMessage with const w = ensureLayoutWorker(); layoutWorkerRef.current = w; w.postMessage(...), or declare the worker-attach effect above the layout effect, or initialize the worker lazily at module scope."},
  {"kind":"bug","severity":"high","title":"Embed-model button uses window.prompt, which is a no-op in Tauri on macOS","file":"src/views/Projects.tsx","lines":"166-181","description":"setModel() collects the embedding model via window.prompt(). Tauri's WKWebView (wry) does not implement the runJavaScriptTextInputPanelWithPrompt WKUIDelegate callback, so on macOS window.prompt() immediately returns null without showing any dialog. The code treats null as cancel and returns, so the 'embed model' button silently does nothing on the app's primary platform — the feature is unreachable.","evidence":"async function setModel(projectId: string, current: string | null) {\n    const input = prompt(...);\n    if (input === null) return;","suggestion":"Replace window.prompt with an in-app modal dialog (extend ConfirmModalHost/useConfirm with a text input). Keep the empty-string-clears semantics."},
  {"kind":"bug","severity":"high","title":"Deleting the active project leaves currentProjectId dangling","file":"src/views/Projects.tsx","lines":"77-97","description":"remove() deletes any project including the currently active one (only 'default' is excluded at the button level) but never resets currentProjectId. The store's fallback in refreshProjects (store.ts:131) only fires when currentProjectId is falsy, and a deleted id is a truthy string, so the selection keeps pointing at the deleted project. Every subsequent store action targets a nonexistent project: memories list renders empty, refreshGraph errors are swallowed by .catch(() => {}) in App.tsx, sidebar keeps highlighting the dead project.","evidence":"await api.deleteProject(id);\n      await refreshProjects();\n      await refreshStats();","suggestion":"After a successful delete of currentProjectId, setCurrentProjectId('default') or projects[0].id before refreshing stats."},
  {"kind":"bug","severity":"medium","title":"Memory search invoke has no catch: failures keep stale results silently","file":"src/views/Memories.tsx","lines":"61-79","description":"The debounced semantic-search effect wraps api.recallExplain in try/finally with no catch. If the invoke rejects, the rejection is unhandled and the UI gives no error signal: setSearching(false) runs in finally, the previous query's results stay on screen, and the user believes the stale list is the answer for the new query.","evidence":"(async () => {\n        setSearching(true);\n        try {\n          const response = await api.recallExplain({ ... });\n        } finally {\n          if (seq === searchSeq.current) setSearching(false);\n        }\n      })();","suggestion":"Add a catch block that clears results/recallId when seq === searchSeq.current and shows showToast({ kind: 'err', ... })."},
  {"kind":"improvement","severity":"medium","title":"Layout worker cannot cancel superseded Barnes-Hut runs; stale runs burn CPU","file":"src/views/layoutWorker.ts","lines":"54-70,357-395","description":"ctx.onmessage handles each layout request synchronously; barnesHutLayout runs up to 120 iterations over all nodes with no abort check, and the worker is never terminated. Graph.tsx claims 'Cancel any prior in-flight request' but the main thread only drops stale results by requestId — the worker still computes every superseded run to completion. Rapidly toggling kind filters queues full-price sequential layouts.","evidence":"ctx.onmessage = (ev) => { ... barnesHutLayout(req.nodes, req.edges, ...); } // no cancellation token","suggestion":"Pass a generation/cancel flag checked in the iteration loop, early-returning when superseded; or terminate+respawn the worker on new requests."},
  {"kind":"bug","severity":"low","title":"Graph layout worker failures are only logged to backend, never shown in UI","file":"src/views/Graph.tsx","lines":"168-170","description":"When the layout worker posts an error, the handler calls send(\"error\", ...) which invokes log_frontend — not the toast system. The user sees nothing: graph silently remains at seed layout.","evidence":"} else if (m.type === \"error\") {\n        send(\"error\", `[graph] layout failed: ${m.message}`);\n      }","suggestion":"Call showToast({ kind: 'err', text: `Graph layout failed: ${m.message}` })."},
  {"kind":"bug","severity":"low","title":"Empty-state 'Build graph' button only re-fetches and toasts false success","file":"src/views/Graph.tsx","lines":"176-189,199-201","description":"When no graph exists, the CTA 'Build graph' calls reload() which only awaits refreshGraph() — a fetch that cannot create a graph — then unconditionally toasts 'Graph refreshed'. For a never-ingested project the button can never succeed at what its label promises.","evidence":"<button onClick={reload} className=\"btn-primary mt-4\">Build graph</button>","suggestion":"Either kick off an ingest or relabel the button 'Refresh' and only toast success when a non-null graph came back."},
  {"kind":"bug","severity":"low","title":"Node hover highlight never cleared when cursor leaves the canvas","file":"src/views/Graph.tsx","lines":"336-338,253-260","description":"onMouseLeave is wired only to onMouseUp which clears dragRef; setHover is never reset. After hovering a node and moving off-canvas, highlight ring + label + dimming stay stuck until pointer re-enters.","evidence":"onMouseUp={onMouseUp}\nonMouseLeave={onMouseUp}","suggestion":"onMouseLeave={() => { dragRef.current = null; setHover(null); }}"},
  {"kind":"bug","severity":"low","title":"Releasing a pan triggers onClick and opens the node under the cursor","file":"src/views/Graph.tsx","lines":"262-269,331-338","description":"No drag-distance threshold between pan and click: any mousemove pans, browser still fires onClick on mouse-up, hitTest runs against moved viewport → finishing a pan over any non-file node opens that memory accidentally.","evidence":"function onClick(e) { ... if (n && n.kind !== \"file\" && n.file_path) { setSelected(n.uid); } }","suggestion":"Track pointer travel in dragRef; ignore click when travel exceeded a few pixels."},
  {"kind":"bug","severity":"low","title":"onWheel preventDefault is a no-op inside React's passive wheel listener","file":"src/views/Graph.tsx","lines":"210-214,330","description":"React 17+ registers onWheel at root as passive, so e.preventDefault() throws 'Unable to preventDefault inside passive event listener' and does nothing. Zoom works but scroll/pinch suppression never happens and every wheel tick logs an error.","evidence":"function onWheel(e: React.WheelEvent) {\n    e.preventDefault();","suggestion":"Attach wheel handler via canvasRef.current.addEventListener('wheel', handler, { passive: false }) in an effect."},
  {"kind":"bug","severity":"low","title":"Ingest progress card shows an arbitrary job when several projects index at once","file":"src/views/Projects.tsx","lines":"33,242-275","description":"activeIngest = Object.values(ingestJobs)[0] picks whichever job inserted first; with two concurrent ingests only one progress card shows and it never says which project it refers to.","evidence":"const activeIngest = Object.values(ingestJobs)[0] as IngestProgress | undefined;","suggestion":"Render one progress card per entry in ingestJobs, each labeled with progress.project_id."},
  {"kind":"bug","severity":"low","title":"Watch toggle duplicates server state into local state that projects refresh clobbers","file":"src/views/Projects.tsx","lines":"27-31,134-148,408","description":"watchOn mirrors p.watch_enabled into local state via effect on [projects]; toggleWatch reads/writes only local copy. Any refreshProjects() landing between setWatch resolving and local setState rebuilds watchOn from stale snapshot and can flip the row back.","evidence":"useEffect(() => { ...setWatchOn(next); }, [projects]);\nawait api.setWatch(projectId, root, enabled);\nsetWatchOn((s) => ({ ...s, [projectId]: enabled }));","suggestion":"Drop the mirror: call api.setWatch then await refreshProjects() and render from p.watch_enabled."},
  {"kind":"bug","severity":"low","title":"Settings copy() swallows clipboard errors with an unhandled rejection","file":"src/views/Settings.tsx","lines":"173-179","description":"copy() chains .then() on clipboard.writeText with no .catch. On failure nothing shown and rejection unhandled — inconsistent with all other clipboard uses which show 'Clipboard blocked'.","evidence":"navigator.clipboard.writeText(text).then(() => { ... });","suggestion":"Add .catch(() => showToast({ kind: 'err', text: 'Clipboard blocked' }))."},
  {"kind":"bug","severity":"low","title":"Parallel MCP installs clobber the single mcpInstalling slot","file":"src/views/Settings.tsx","lines":"189-201,345-364","description":"mcpInstalling is one string; clicking a second target while first is in flight overwrites state, first button re-enables mid-flight, and whichever invoke settles first clears the other's installing state.","evidence":"setMcpInstalling(target); try { ... } finally { setMcpInstalling(null); }","suggestion":"Track in-flight targets as Set<string>, disable all buttons while non-empty, or serialize installs."},
  {"kind":"improvement","severity":"low","title":"MCP 'installed' checkmarks are session-only and never detect existing configs","file":"src/views/Settings.tsx","lines":"36,345-364","description":"mcpInstalled starts empty and only grows after successful install this session. On relaunch all five targets render as not-installed even when configs exist.","evidence":"const [mcpInstalled, setMcpInstalled] = useState<Set<string>>(new Set());","suggestion":"On mount probe each target (dry-run/status variant of install_mcp_config) to seed mcpInstalled."},
  {"kind":"bug","severity":"low","title":"Fire-and-forget invokes (recall feedback, load more) have no error handling","file":"src/views/Memories.tsx","lines":"288-289,296-305,38-42","description":"Three invokes launched with void and no .catch: implicit recall feedback, explicit feedback .then(toast) without rejection path, handleLoadMore try/finally discards error. Backend failures produce unhandled rejections and zero user feedback.","evidence":"void api.submitRecallFeedback(recallId, m.uid, 1, \"implicit\");","suggestion":"Add .catch handlers that showToast({ kind: 'err', ... })."},
  {"kind":"improvement","severity":"low","title":"Pagination indicator hidden above 200 memories and page math is misleading","file":"src/views/Memories.tsx","lines":"324-328","description":"Footer shows '· page N' only while memories.length < 200 — hides indicator exactly when pagination matters most. Math.floor(memoryOffset / 50) reads 'page 1' after first load-more while viewing page 1 content from offset 0.","evidence":"{!query && memories.length < 200 && (<> · page {Math.floor(memoryOffset / 50) || 1}</>)}","suggestion":"Show 'X loaded' plus page count computed from total, or simply memories.length loaded."},
  {"kind":"improvement","severity":"low","title":"Settings claims the MCP server exposes 20 tools; it exposes 27","file":"src/views/Settings.tsx","lines":"372-376","description":"MCP section copy says 'your agent has 20 tools' but server registers 27 tools including update, get_memory, list, recall_explain, operations tools, etc. Stale count misleads users configuring agents.","evidence":"Once connected, your agent has 20 tools (search, remember, forget, ingest_project, consolidate, list_projects, …).","suggestion":"Update copy to 27 or derive count from schema at build time."}
]
