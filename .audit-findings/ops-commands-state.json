[
  {"kind":"bug","severity":"high","title":"Fix TOCTOU in get_or_load_index that double-opens the same project index","file":"src-tauri/src/state.rs","lines":"143-179","description":"Cache miss check and insert are not atomic: two threads calling get_or_load_index for the same cold project both see no entry, both call ProjectIndex::open_or_create (which takes no file lock), and both insert. Result: two live ProjectIndex instances over the same .tvim/.uidmap.json with independent next_extid counters; each assigns colliding extids and whichever flushes last clobbers the other's entries. Trigger: two concurrent search/remember calls for a project not yet loaded (the MCP server serves concurrent tool calls). Data resurfaces only after digest-mismatch repair re-embeds everything.","evidence":"let indices = self.indices.read();\nif let Some(idx) = indices.get(project_id).cloned() { ... }\n// miss path, outside any lock:\nlet idx = Arc::new(ProjectIndex::open_or_create(...)?);\nlet mut indices = self.indices.write();\nindices.insert(project_id.to_string(), idx.clone());","suggestion":"Hold indices.write() across the load, or use entry-style double-checked insert under the write lock, or add an exclusive flock per .tvim inside open_or_create."},
  {"kind":"bug","severity":"high","title":"Stop evicting in-use indices whose dirty writes are then never flushed","file":"src-tauri/src/state.rs","lines":"200-227","description":"evict_if_over_budget/evict_stale_indices remove an Arc<ProjectIndex> from the map while callers still hold clones. The flusher thread and flush_all_indices only iterate map contents, and ProjectIndex has no Drop flush, so post-eviction adds live only in the detached instance and are lost on drop until digest-mismatch repair rebuilds. Worst case: a single project whose index exceeds the 512 MiB budget — get_or_load_index inserts it and then immediately calls evict_if_over_budget (line 178), evicting the just-loaded index, so every returned Arc is detached and nothing ever persists via the flusher.","evidence":"let mut indices = self.indices.write();\nindices.remove(&pid);\n...\nlet _ = self.evict_if_over_budget(); // end of get_or_load_index, after insert","suggestion":"Flush before eviction (idx.flush() on evict), skip pids still strongly referenced (Arc::strong_count check), and never evict the index inserted by the current call."},
  {"kind":"bug","severity":"high","title":"Make install_mcp_config atomic and stop wiping configs on JSON parse errors","file":"src-tauri/src/commands.rs","lines":"552-585","description":"For cursor/windsurf/claude targets, existing config parsed with unwrap_or_else(|_| json!({})): if ~/.claude.json (which holds substantial Claude Code state) fails to parse, the whole file is overwritten with just mcpServers.biturbo, destroying everything else. Writes use std::fs::write directly (no temp+rename), so a crash mid-write truncates the user's config.","evidence":"serde_json::from_str(&content).unwrap_or_else(|_| serde_json::json!({}))\n...\nstd::fs::write(&config_path, output).map_err(|e| ...)","suggestion":"Fail loudly on parse errors instead of defaulting to {}, write temp+rename, keep a one-shot .bak of the original."},
  {"kind":"bug","severity":"high","title":"Fix TOML section removal prefix match that deletes neighboring codex sections","file":"src-tauri/src/commands.rs","lines":"634-644","description":"The skip-reset condition uses starts_with(\"[mcp_servers.biturbo\"), so a section like [mcp_servers.biturbo_extra] does NOT reset the skip flag; every line of that foreign section is then stripped from the user's ~/.codex/config.toml until the next unrelated header. Silent data loss in another tool's config.","evidence":"if trimmed.starts_with('[') && !trimmed.starts_with(\"[mcp_servers.biturbo\") {\n    skip = false;\n}","suggestion":"Reset skip on any line starting with '[' except exactly \"[mcp_servers.biturbo]\" (compare the exact header)."},
  {"kind":"bug","severity":"medium","title":"Replace expect in refresh_indices that panics the app at startup","file":"src-tauri/src/state.rs","lines":"133-138","description":"refresh_indices runs during AppState::open and panics via .expect if ProjectIndex::open_or_create fails (permissions, disk full, corrupt dir). A single unwritable index file crashes the whole app instead of degrading to an error for that project.","evidence":"let idx = ProjectIndex::open_or_create(&pid, dim, bw as usize, &data_dir)\n    .expect(\"create project index\");","suggestion":"Log and skip the failing project (or propagate BiResult from AppState::open); never panic on I/O errors."},
  {"kind":"improvement","severity":"medium","title":"Move whole-project re-embedding out of the synchronous search path","file":"src-tauri/src/state.rs","lines":"301-372","description":"embed_and_search calls repair_index_if_needed before every search; on count/digest mismatch it synchronously embed-batches every active memory of the project and replace_alls the index inline. A stale index turns a millisecond query into a multi-second blocking call, and two concurrent searches that both detect staleness duplicate the entire rebuild. No per-project guard against concurrent repairs.","evidence":"pub fn embed_and_search(...) {\n    self.repair_index_if_needed(project_id)?;\n    ...\n}","suggestion":"Do incremental replay for small deltas, run full rebuilds on a background worker with a per-project in-flight guard, serve slightly-stale results meanwhile."},
  {"kind":"bug","severity":"medium","title":"Guard operation status transitions so terminal rows cannot be overwritten","file":"src-tauri/src/operations.rs","lines":"859-885","description":"update_status (complete/fail/mark_cancelled) and update_progress UPDATE with no status predicate, so anything can overwrite a terminal row: a late progress tick flips phase/current on a 'succeeded' op, and a racing fail() after complete() rewrites the outcome. mark_running enforces 'queued' but no other transition is enforced.","evidence":"\"UPDATE operations SET status = ?1, ... WHERE id = ?8\"","suggestion":"Add AND status IN ('queued','running') to update_status/update_progress; treat changed==0 as no-op for progress ticks."},
  {"kind":"bug","severity":"medium","title":"Preserve watch_ingest kind when retrying a cancelled watch ingest","file":"src-tauri/src/operations.rs","lines":"333-341","description":"retry maps both 'ingest' and 'watch_ingest' to start_ingest, which always creates a new operation with kind 'ingest'. A cancelled/failed watch_ingest retried from the UI silently becomes a one-shot ingest; watching semantics lost with no indication.","evidence":"match operation.kind.as_str() {\n    \"ingest\" | \"watch_ingest\" => { ... start_ingest(state, &project_id, &root) }","suggestion":"Route watch_ingest retries through the watch_ingest creation path or reject retry with a clear message."},
  {"kind":"bug","severity":"medium","title":"Fail the operation when executor thread spawn fails instead of orphaning it queued","file":"src-tauri/src/operations.rs","lines":"270-277","description":"All background starters (start_multi_ingest, start_consolidate, start_model_rebuild, spawn_ingest, and all four resume_pending arms) discard the spawn result with .ok(). If Builder::spawn fails, the operation stays 'queued' forever: no executor exists, request_cancel sets a flag nobody reads, UI polls a job that will never run.","evidence":"std::thread::Builder::new()\n    .name(format!(\"biturbo-operation-{id}\"))\n    .spawn(move || { ... })\n    .ok();","suggestion":"Match on the spawn Result and call fail(state, &id, ...) when it errs."},
  {"kind":"bug","severity":"medium","title":"Clamp list_memories limit before it reaches SQL LIMIT","file":"src-tauri/src/commands.rs","lines":"19-33","description":"The IPC command forwards limit.unwrap_or(50) straight into memory::list, which pushes limit as i64 into LIMIT with no clamp. A frontend bug or hostile renderer can pass usize::MAX and force materializing the entire memories table into a serialized response. operations::list already clamps(1,500); this boundary does not.","evidence":"limit.unwrap_or(50),\noffset.unwrap_or(0),","suggestion":"Clamp at the command boundary, e.g. limit.unwrap_or(50).clamp(1, 500) (and bound offset)."},
  {"kind":"improvement","severity":"medium","title":"Restrict export_memories output path to user-selected locations","file":"src-tauri/src/commands.rs","lines":"336-352","description":"export_memories writes JSON to any renderer-supplied output_path with zero validation. Any XSS/renderer compromise turns this into arbitrary file overwrite of user-writable files (dotfiles, shell rc).","evidence":"crate::io::export_memories(state.inner(), args.project_id.as_deref(), std::path::Path::new(&args.output_path))","suggestion":"Require the path come from a dialog plugin pick, or restrict to an exports subdir under the data dir."},
  {"kind":"bug","severity":"low","title":"Close embedder_for_project cache race and stop mapping DB errors to NotFound","file":"src-tauri/src/state.rs","lines":"374-401","description":"Check-then-insert on project_embedders is racy: two threads can both miss and each build Embedder::new(model) (full ONNX session load), discarding one — memory/CPU spike under concurrency. Additionally the model lookup maps ANY query_row error to NotFound, so transient SQLite busy/locked errors are misreported as missing project.","evidence":"if let Some((cached_model, embedder)) = self.project_embedders.read().get(project_id) {...}\nlet embedder = Arc::new(Embedder::new(&model)?);","suggestion":"Build under the write lock (double-check after acquiring); distinguish QueryReturnedNoRows from other errors."},
  {"kind":"improvement","severity":"low","title":"Give the index-flusher thread a shutdown flag and per-instance lifetime","file":"src-tauri/src/state.rs","lines":"90-110","description":"AppState::open spawns an infinite 'biturbo-index-flusher' loop with no exit condition, holding an AppState clone forever. Every AppState::open leaks another 5-second-tick thread — including every unit test that builds a state.","evidence":"std::thread::Builder::new()\n    .name(\"biturbo-index-flusher\".into())\n    .spawn(move || loop { std::thread::sleep(...); ... })","suggestion":"Gate the loop on an AtomicBool stopped on Drop, or share one flusher keyed by data_dir."},
  {"kind":"improvement","severity":"low","title":"Avoid holding the indices read lock across flush disk I/O","file":"src-tauri/src/state.rs","lines":"280-285","description":"flush_all_indices holds indices.read() for the whole loop while maybe_flush writes temp files and renames. Large indexes block writers (get_or_load insert, eviction, model-rebuild map removal) for the duration of fsync-class I/O.","evidence":"pub fn flush_all_indices(&self) {\n    let indices = self.indices.read();\n    for idx in indices.values() { ... }","suggestion":"Clone the Arcs out under a short read guard, drop the lock, then flush each index."},
  {"kind":"bug","severity":"low","title":"Honor cancellation during long consolidations, not only before and after","file":"src-tauri/src/operations.rs","lines":"558-585","description":"execute_consolidate checks is_cancel_requested only before starting and after consolidate returns; consolidate receives no operation id, so a minutes-long consolidation ignores cancel_requested entirely — UI cancel appears to do nothing until work finishes.","evidence":"if is_cancel_requested(state, id)? { ... }\nmatch crate::consolidate::consolidate(state, project_id) {","suggestion":"Thread the operation id into consolidate and poll is_cancel_requested between clusters/batches, aborting with mark_cancelled."},
  {"kind":"bug","severity":"low","title":"Report real elapsed time in multi-ingest done event instead of hardcoded 0","file":"src-tauri/src/operations.rs","lines":"543-552","description":"The multi-ingest:done payload always emits elapsed_ms: 0 because the literal is hardcoded; consumers timing jobs get garbage.","evidence":"serde_json::json!({ \"job_id\": id, ..., \"elapsed_ms\": 0, ... })","suggestion":"Capture Instant::now() at start and emit started.elapsed().as_millis()."},
  {"kind":"improvement","severity":"low","title":"Clean up per-operation lock files that accumulate forever","file":"src-tauri/src/operations.rs","lines":"842-850","description":"Every operation creates operation-locks/{id}.lock on disk and nothing ever deletes these files. Unbounded clutter (one file per ingest/consolidate/retry ever run) in the app data dir.","evidence":"fn open_operation_lock(state: &AppState, id: &str) -> BiResult<std::fs::File> { ... }","suggestion":"Delete the lock file after releasing (terminal ops), or prune locks older than N days at startup."},
  {"kind":"bug","severity":"low","title":"Remove leftover shadow_dir when model rebuild activation fails","file":"src-tauri/src/operations.rs","lines":"725-736","description":"On activate error the handler restores backups but never removes shadow_dir (rebuild-{id}), unlike success/cancel/mismatch paths. Each failed activation strands a fully built shadow index (potentially hundreds of MB) in indices/ forever.","evidence":"if let Err(error) = activate {\n    std::fs::remove_file(&actual).ok();\n    ... return Err(error);\n}   // no remove_dir_all(&shadow_dir) here","suggestion":"Call remove_dir_all(&shadow_dir) in the activate-error branch too; sweep stale rebuild-* dirs at startup."},
  {"kind":"bug","severity":"low","title":"Resume more than 500 queued operations and preserve malformed multi-ingest entries","file":"src-tauri/src/operations.rs","lines":"398-456","description":"resume_pending scans only list(&state, 500): with >500 queued operations the remainder are never resumed after restart. The multi_ingest arm uses filter_map that silently drops checkpoint entries missing fields, resuming a partial project list without error.","evidence":"let pending = list(&state, 500)?;\n...\n.filter_map(|project| { Some((project.get(\"project_id\")?.as_str()?.to_string(), ...)) })","suggestion":"Page through all queued ops (WHERE status='queued'); fail or log loudly when checkpoint entries are malformed."},
  {"kind":"bug","severity":"low","title":"Distinguish genuine failures from cancellations in ingest error handling","file":"src-tauri/src/operations.rs","lines":"817-830","description":"The Err branch decides recorded status solely via is_cancel_requested: if a real error occurs while the user also hit cancel, the operation is recorded 'cancelled' with no error text persisted, hiding the actual failure from list_operations/retry diagnostics.","evidence":"Err(error) => {\n    let cancelled = is_cancel_requested(state, id).unwrap_or(false);\n    if cancelled { mark_cancelled(state, id)?; } else { fail(state, id, &error.to_string())?; }","suggestion":"Only record 'cancelled' when the error is the cancellation sentinel; otherwise fail() with the real error."},
  {"kind":"improvement","severity":"low","title":"Throttle update_progress DB writes and event emissions per batch","file":"src-tauri/src/operations.rs","lines":"100-133","description":"Every progress tick performs a full write transaction plus follow-up SELECT plus Tauri event emit. Ingest calls this per batch, so large projects generate thousands of transactions and events; each tick rewrites checkpoint via COALESCE even when unchanged. Write amplification slows ingests and floods the frontend event queue.","evidence":"state.db.write(|tx| { tx.execute(\"UPDATE operations SET ...\", ...) })?;\nif let (Some(app), Ok(operation)) = (&state.app, get(state, id)) { ... }","suggestion":"Emit progress at most every N ms or M% change; fold read-back into UPDATE ... RETURNING."},
  {"kind":"bug","severity":"low","title":"Stop fabricating scheduler queued=true in consolidate_now response","file":"src-tauri/src/commands.rs","lines":"254-262","description":"consolidate_now starts the operation via operations::start_consolidate (independent of the scheduler) then mutates scheduler::get_status() output to claim queued=true. Returned ConsolidateStatus lies about scheduler state; command returns no operation id so callers cannot correlate with operation_status.","evidence":"crate::operations::start_consolidate(...)?;\nlet mut status = crate::scheduler::get_status();\nstatus.queued = true;","suggestion":"Return the created Operation or derive ConsolidateStatus truthfully; never hand-mutate scheduler state."},
  {"kind":"improvement","severity":"low","title":"Consolidate duplicate ingest_project and start_ingest IPC commands","file":"src-tauri/src/commands.rs","lines":"197-217","description":"Two commands do exactly the same thing (both call operations::start_ingest) differing only in return type. Dead-weight API surface invites drift: fixes applied to one path miss the other.","evidence":"pub fn ingest_project(...) -> BiResult<IngestJobResponse> { ... }\npub fn start_ingest(...) -> BiResult<Operation> { ... }","suggestion":"Keep one command returning Operation (job_id derivable) and migrate the frontend."},
  {"kind":"bug","severity":"low","title":"Validate root_path existence in set_watch like the ingest commands do","file":"src-tauri/src/commands.rs","lines":"356-380","description":"When enabling with explicit root_path, set_watch passes the path straight to enable_watch without the is_dir()/exists() check other commands perform. A typo'd or deleted directory spawns a watcher over nothing and silently overrides the project's stored root_path.","evidence":"let root = if let Some(r) = args.root_path.as_ref() { std::path::PathBuf::from(r) } else { ... };\ncrate::io::enable_watch(state.inner(), &args.project_id, &root)?;","suggestion":"Reject nonexistent/non-directory roots with BiError::Invalid before enabling."}
]
