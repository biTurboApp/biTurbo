[
  {"kind":"bug","severity":"high","title":"explain() panics on empty typed candidate set via turbovec empty-allowlist assert","file":"src-tauri/src/recall.rs","lines":"56-67","description":"explain() builds an allowlist of uids for the requested mem_type and passes it to embed_and_search without the empty-check that memory::search has (memory.rs:306-308). When the type has zero active memories in the project, allowlist = Some([]) reaches ProjectIndex::search -> turbovec IdMapIndex::search_with_allowlist, which does assert!(!ids.is_empty(), \"allowlist is empty\") (turbovec-0.8.0/src/id_map.rs:209). Trigger: MCP/UI recall_explain with a mem_type that has no rows -> panic in the command thread instead of an empty result.","evidence":"let allowlist: Option<Vec<String>> = if let Some(mem_type) = mem_type { ... Some(rows.filter_map(Result::ok).collect()) } ... state.embed_and_search(project_id, query, candidate_k, allowlist.as_deref())?;","suggestion":"Add the same guard as memory::search: if allowlist.as_ref().is_some_and(Vec::is_empty) return an empty RecallResponse before calling embed_and_search."},
  {"kind":"bug","severity":"high","title":"Filter padded sentinel search hits (score -inf, ext_id 0) out before score fusion","file":"src-tauri/src/index_engine.rs","lines":"228-236","description":"turbovec's search pads every result row to exactly k entries with (score = f32::NEG_INFINITY, id = 0) (turbovec-0.8.0/src/search.rs). External ids are slots starting at 0, so id 0 maps to a real uid, and index_engine's filter_map converts each pad entry into a SearchHit{uid_of_first_vector, score: -inf}. fuse_rankings clamps -inf to similarity 0.0 and ACCUMULATES a rank score per entry, so with kk=30 and only e.g. 3 candidates, the first-inserted memory gets ~27 extra contributions (~+0.22) versus ~0.016 for a genuine top hit — it dominates every typed/small-project search and its rank shows up as #1 in recall explanations.","evidence":"let allowlist_extids ... .filter_map(|(id, score)| { inner.extid_to_uid.get(&id).map(|uid| SearchHit { uid: uid.clone(), score, ext_id: id }) })  // pad entries pass straight through","suggestion":"In ProjectIndex::search, drop entries whose score == f32::NEG_INFINITY before mapping to SearchHit."},
  {"kind":"bug","severity":"high","title":"Stop re-applying full age-decay to already-decayed importance on every consolidate run","file":"src-tauri/src/consolidate.rs","lines":"98-106","description":"apply_decay computes new_imp = stored_importance * exp(-age_since_created_at/half_life) + boost. The decay factor is the TOTAL decay since creation, but it is multiplied into the already-decayed stored value, so the operation is not idempotent: after n runs importance ~= imp0*d^n + b*(d^(n-1)+...+1). The scheduler runs consolidate automatically every 6 hours plus at startup and on demand, so a 60-day-old memory (d~0.37) is driven to the 0.05 floor after ~3 runs regardless of original importance, while any memory accessed in the last 30 days inflates toward the 1.0 clamp. Decay destroys good memories and access stats get amplified into certainty.","evidence":"let decay = (-age_ms / half_life_ms as f64).exp();\nlet new_imp = (importance * decay + boost).clamp(0.05, 1.0) as f32;","suggestion":"Store the original/base importance and compute decayed value from created_at each run, or apply incremental decay over elapsed time since last application."},
  {"kind":"bug","severity":"high","title":"Exclude superseded memories from dedup so merge cannot delete the active copy","file":"src-tauri/src/consolidate.rs","lines":"150-176","description":"find_duplicates pages memories with memory::list, which has no `superseded_by IS NULL` filter (verified: memory.rs:511-528). A superseded zombie A and its active successor B are near-identical by design, so they routinely exceed the 0.95 cosine threshold. keep/drop chosen purely by importance: if A.importance >= B.importance, merge_pair keeps A, marks B superseded_by=A.id and hard-deletes B. Result: the active memory is deleted and survivor A still carries its stale superseded_by pointer so it is excluded from all searches — both memories effectively disappear.","evidence":"let mems: Vec<Memory> = memory::list(state, Some(&pid), None, BATCH, offset)?;  // includes superseded rows\nlet (keep, drop_) = if a.importance >= b.importance { (a.uid.clone(), b.uid.clone()) } else { ... };","suggestion":"Filter superseded_by IS NULL in find_duplicates (and re-verify inside merge_pair inside one transaction) so only active memories can be merged."},
  {"kind":"bug","severity":"medium","title":"Clear supersession pointers in forget() so the predecessor is not hidden forever","file":"src-tauri/src/memory.rs","lines":"198-217","description":"remember(supersedes=A) sets A.superseded_by = new_id. forget(new) only does DELETE FROM memories WHERE uid = ?1 and never clears pointers, leaving A.superseded_by dangling. A stays in the DB (counted by list/count_by_type) but is permanently excluded from every search and vector index because of the superseded_by IS NULL filters. Trigger: remember B superseding A, then forget B — A silently vanishes from recall forever.","evidence":"tx.execute(\"DELETE FROM memories WHERE uid = ?1\", rusqlite::params![uid])?;  // no UPDATE memories SET superseded_by = NULL","suggestion":"In forget(), after resolving the row id, run UPDATE memories SET superseded_by = NULL WHERE superseded_by = ?old_id in the same transaction."},
  {"kind":"bug","severity":"medium","title":"Prune recall_events/recall_feedback or they grow unboundedly","file":"src-tauri/src/recall.rs","lines":"113-134","description":"Every explain() inserts a recall_events row containing result_uids plus a full serialized explanations JSON, and there is no DELETE/retention anywhere in the codebase. The MCP server instructions tell agents to call recall_for_context on every user message, so this table grows by one large JSON row per message forever, bloating the SQLite file with data nothing reads except submit_feedback's single lookup.","evidence":"tx.execute(\"INSERT INTO recall_events(id, project_id, query_hash, result_uids, explanations, created_at) VALUES(?1, ?2, ?3, ?4, ?5, ?6)\", ...)?;  // no pruning path exists","suggestion":"Add a retention policy (delete events older than N days or keep last M per project); consider storing explanations without full per-hit structs."},
  {"kind":"bug","severity":"medium","title":"Use one tokenizer for reranking so explanations match actual ranking","file":"src-tauri/src/memory.rs","lines":"587-595","description":"search() reranks with tokenize_query (lowercase only, punctuation kept, len>=2), while explain() computes matched_terms and applied_boosts with normalized_terms (recall.rs:301-311), which strips edge punctuation/underscores. For query 'rust!' the search path boosts nothing but the explanation reports a content match for 'rust' and a multiplier that was never applied — the explainability feature misreports the actual scoring.","evidence":"fn tokenize_query(query: &str) -> Vec<String> {\n    query.split_whitespace().map(|t| t.to_lowercase()).filter(|t| t.len() >= 2).collect()\n}","suggestion":"Delete tokenize_query and use recall::normalized_terms (or shared helper) in memory::search so both paths score identically."},
  {"kind":"bug","severity":"medium","title":"Require word-boundary tag matches instead of bidirectional substrings","file":"src-tauri/src/recall.rs","lines":"178-185","description":"Tag matching accepts tag.contains(term) || term.contains(&tag) with no length floor, so a query term 'rust' matches tag 'us', 'logging' matches tag 'go', and any 1-2 char tag matches most queries. Each false match multiplies the fused score by 1.20 and is reported as a tag match in explanations, systematically distorting ranking toward memories with short/generic tags.","evidence":"memory.tags.iter().any(|tag| {\n    let tag = tag.to_lowercase();\n    tag.contains(term.as_str()) || term.contains(&tag)\n})","suggestion":"Match tags on equality or word-boundary containment; ignore tags shorter than ~3 chars for substring matching."},
  {"kind":"improvement","severity":"medium","title":"Recalibrate importance/feedback boosts against the fused-score scale","file":"src-tauri/src/recall.rs","lines":"224-230","description":"Fused RRF scores are bounded by roughly 2/61 (~0.033) for a #1 vector+#1 FTS hit, but apply_ranking_boost adds importance*0.05 (up to +0.05 flat) and search adds feedback boosts up to +/-0.02. These additive terms exceed the entire achievable relevance signal, so after reranking, ordering is dominated by the importance field and one feedback click rather than by query relevance; a max-importance memory with zero term matches outranks a perfect relevance match.","evidence":"base_score * boosts.multiplier + boosts.importance_boost  // importance_boost up to 0.05 vs base_score <= ~0.033","suggestion":"Scale additive boosts relative to the RRF score range or normalize scores before the rerank stage."},
  {"kind":"bug","severity":"medium","title":"Reject unresolvable supersedes references instead of silently inserting a non-superseding memory","file":"src-tauri/src/memory.rs","lines":"120-130","description":"When input.supersedes is set but the SELECT finds no row (unknown uid, other project, or already superseded), superseded is None and the insert proceeds with supersedes=NULL — no error returned. The caller believes the old memory was replaced, but both memories remain active and searchable, duplicating possibly contradictory facts.","evidence":"let superseded: Option<(i64, String)> = match input.supersedes.as_deref() {\n    Some(old_uid) => tx.query_row(...).optional()?,\n    None => None,\n};","suggestion":"If input.supersedes.is_some() and lookup returns None, return BiError::Invalid naming the uid."},
  {"kind":"improvement","severity":"medium","title":"Guard auto-dedup against near-duplicate-but-distinct memories (configurable threshold + content diff)","file":"src-tauri/src/consolidate.rs","lines":"164-176","description":"The hardcoded 0.95 cosine threshold merges memories that differ by a single token: BGE-small embeddings for 'deploy to staging every Friday' vs 'deploy to production every Friday' score well above 0.95, yet the facts differ. merge_pair unions only tags and permanently hard-deletes the other memory's content, and consolidate runs automatically every 6 hours, so one bad embedding pair silently destroys data with no way to configure or review.","evidence":"if h.score < 0.95 || h.uid == a.uid { continue; }\n...\nmemory::forget(state, drop_uid)?;  // content gone for good","suggestion":"Make threshold configurable, require minimal token-level content diff check before merging, consider soft-retiring instead of hard forget during automated runs."},
  {"kind":"bug","severity":"low","title":"Remove dead supersede UPDATE in merge_pair (row is deleted immediately after)","file":"src-tauri/src/consolidate.rs","lines":"205-218","description":"merge_pair writes UPDATE memories SET superseded_by = ... WHERE uid = drop_uid then immediately calls memory::forget(drop_uid) which hard-DELETEs the row. The UPDATE is always discarded — wasted work and misleading code suggesting soft-supersede semantics that do not exist. The 'merge' also drops the dropped memory's access_count/importance entirely rather than folding them into the survivor.","evidence":"tx.execute(\"UPDATE memories SET superseded_by = (SELECT id FROM memories WHERE uid = ?1), ...\", ...)?;\nmemory::forget(state, drop_uid)?;","suggestion":"Drop the UPDATE (or make forget soft) and fold access_count/max(importance) from dropped memory into keeper inside same transaction."},
  {"kind":"bug","severity":"low","title":"Replace unwrap in embed_batch with an error on embedding-count mismatch","file":"src-tauri/src/embed.rs","lines":"96-104","description":"embed_batch zips missing inputs with computed results then does cached.into_iter().map(|o| o.unwrap()). If fastembed ever returns fewer vectors than input texts, the Option unwrap panics the thread instead of returning BiError. Invariant upheld only by trust in the dependency.","evidence":"Ok(cached.into_iter().map(|o| o.unwrap()).collect())","suggestion":"Check computed.len() == missing.len() and return BiError::Embed on mismatch instead of unwrapping."},
  {"kind":"bug","severity":"low","title":"Re-check model under EMBED_LOCK to avoid spurious 'model released mid-embed' failures","file":"src-tauri/src/embed.rs","lines":"123-128","description":"ensure_model() runs BEFORE the EMBED_LOCK is taken. The scheduler calls release_idle_embedders() every 120s and release_if_idle nulls self.model under EMBED_LOCK; a thread that passed ensure_model's read check can then acquire EMBED_LOCK, observe model == None and fail the whole embed/search with BiError::Embed(\"model released mid-embed\") instead of lazily reloading. Users see sporadic semantic-search failures right after idle periods.","evidence":"self.ensure_model()?;\nlet _guard = EMBED_LOCK.lock();\nlet model = guard.as_ref().ok_or_else(|| BiError::Embed(\"model released mid-embed\".into()))?;","suggestion":"After acquiring EMBED_LOCK, if model is None call ensure_model() again rather than erroring."},
  {"kind":"improvement","severity":"low","title":"Scope the embedding mutex per Embedder instead of one global EMBED_LOCK","file":"src-tauri/src/embed.rs","lines":"17-124","description":"embed_batch_uncached_stream, release_if_idle and force_release all take the process-wide static EMBED_LOCK, serializing ONNX inference across ALL projects and Embedder instances. This contradicts the struct comment ('RwLock (not Mutex)... concurrent embeds only need a read lock'): concurrent searches/ingests for different projects cannot overlap even though each TextEmbedding session is thread-safe.","evidence":"static EMBED_LOCK: Lazy<Mutex<()>> = Lazy::new(|| Mutex::new(()));","suggestion":"Move the lock into Embedder so independent embedders can run concurrently; keep a global cap only if ONNX thread pools oversubscribe CPU."},
  {"kind":"improvement","severity":"low","title":"Drop the dead AND fallback and stop swallowing FTS errors in fts_search","file":"src-tauri/src/memory.rs","lines":"413-433","description":"fts_search tries OR-combined query and falls back to AND-combined when OR returns no hits. An AND query is semantically a subset of OR, so if OR matched nothing AND cannot either — the fallback's only live path is when the OR query ERRORED, and that error is silently discarded by `if let Ok(hits)`, masking real FTS problems instead of logging them.","evidence":"if !or_query.is_empty() {\n    if let Ok(hits) = run_fts_query(conn, &or_query, ...) {\n        if !hits.is_empty() { return Ok(hits); }\n    }\n}","suggestion":"Remove the AND pass (or invert: try AND first for precision), and log-or-propagate the OR query error."},
  {"kind":"bug","severity":"low","title":"Exclude superseded memories from count_by_type stats","file":"src-tauri/src/memory.rs","lines":"540-558","description":"count_by_type has no superseded_by IS NULL predicate, so stats include zombie rows that search, the vector index and repair_index_if_needed all exclude. After heavy supersede usage the reported per-type counts drift upward from what is actually retrievable.","evidence":"\"SELECT mem_type, COUNT(*) FROM memories WHERE project_id = ?1 GROUP BY mem_type\"","suggestion":"Add AND superseded_by IS NULL to both variants (or expose active vs total explicitly)."},
  {"kind":"bug","severity":"low","title":"Compute decay deltas inside the write transaction to avoid lost updates","file":"src-tauri/src/consolidate.rs","lines":"49-121","description":"apply_decay reads all importances via a pooled connection, computes new values outside any transaction, then writes them in a second step. A concurrent memory::update() that sets importance between read and write is silently overwritten by the stale computed value (read-modify-write race).","evidence":"let rows: Vec<(String, f64, i64, i64, i64)> = ... // snapshot outside txn\nstate.db.write(|tx| { ... })?;","suggestion":"Do SELECT and UPDATEs inside one write transaction, or use UPDATE ... SET importance = importance*?delta SQL so arithmetic happens atomically."},
  {"kind":"bug","severity":"low","title":"Validate non-empty content in update() like remember() does","file":"src-tauri/src/memory.rs","lines":"219-232","description":"remember() rejects whitespace-only content but update() stores input.content verbatim, so update(uid, content: Some(\" \")) produces a whitespace-only memory that still occupies the vector index and FTS row, degrading search results and contradicting the write-path invariant.","evidence":"let new_content = input.content.clone().unwrap_or_else(|| existing.content.clone());  // no trim/is_empty validation","suggestion":"Apply the same trim().is_empty() rejection (or treat empty as 'keep existing') in update()."},
  {"kind":"improvement","severity":"low","title":"Use embed_batch_uncached for bulk dedup embedding to avoid thrashing the query cache","file":"src-tauri/src/consolidate.rs","lines":"156-159","description":"find_duplicates embeds the whole non-code corpus with embed_batch, which inserts every corpus text into the shared 256-entry LRU query cache. embed_batch_uncached was added specifically to skip the LRU cache to avoid pollution, yet this bulk path pollutes it anyway, evicting real query embeddings right after every consolidate run.","evidence":"let embeddings = state.embedder_for_project(&pid)?.embed_batch(&texts)?;  // cached variant for bulk corpus pass","suggestion":"Call embed_batch_uncached here (same for rebuild loop in state.rs repair_index_if_needed)."},
  {"kind":"improvement","severity":"low","title":"Cache uid_digest between mutations instead of re-sorting/hashing all uids on every search","file":"src-tauri/src/index_engine.rs","lines":"277-281","description":"embed_and_search calls repair_index_if_needed on every query, and repair compares idx.uid_digest() against index_state. uid_digest collects every uid, sort_unstable()s and hashes them — O(n log n) string work per search. With large indexes (100k+ code chunks) this adds measurable per-query latency for a value that only changes when the index changes.","evidence":"pub fn uid_digest(&self) -> String {\n    let inner = self.inner.lock();\n    let mut uids: Vec<&str> = inner.uid_to_extid.keys().map(String::as_str).collect();\n    uids.sort_unstable();","suggestion":"Maintain digest incrementally (recompute after add_batch/remove/replace_all, memoize behind dirty flag)."}
]
