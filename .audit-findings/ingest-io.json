[
  {"kind":"bug","severity":"high","title":"Stop walking .git and hidden dirs: hidden(false) has no .git exclusion","file":"src-tauri/src/ingest.rs","lines":"212-222","description":"The ingest walk sets .hidden(false), which disables the only filter that excludes dot-directories. Verified against ignore 0.4.31 source: there is NO hardcoded .git skip — has_git only gates require_git semantics. So the walker descends into .git/ (objects, hooks) on every ingest: every loose object is stat()'d via e.path().is_file() (tens of thousands of stats on big repos), and any file inside .git with a recognized extension gets parsed and indexed. Worse, in folders without .git, require_git (default true) means NO gitignore filtering applies at all, so hidden dirs like .venv/, .tox/, .mypy_cache/ are fully indexed. build_structure_summary uses hidden(true) (line 1495), so the UI structure tree contradicts what is actually indexed.","evidence":"WalkBuilder::new(root)\n    .follow_links(false)\n    .standard_filters(true)\n    ...\n    .add_custom_ignore_filename(\".biturboignore\")\n    .hidden(false)","suggestion":"Add an explicit filter excluding path components named .git (and consider .jj), or use .filter_entry; alternatively add \".git/\" to the generated .biturboignore. Align build_structure_summary's hidden setting with the ingest walk."},
  {"kind":"bug","severity":"high","title":"Fix graph file-node UIDs: absolute file_path breaks edge connectivity","file":"src-tauri/src/ingest.rs","lines":"685-695","description":"Chunks are stored with an ABSOLUTE file_path (parse_one_file line 1022), and edges reference rel-based UIDs built as '{project_id}::file::{rel}'. get_project_graph rebuilds the file-node UID from the stored absolute path using only trim_start_matches('/'), producing proj::file::Users/me/proj/src/a.rs. These node UIDs never match the from_uid/to_uid values written by ingest (member_of and imports edges), so in the graph view every file node is orphaned from its chunks and all edges dangle — the code graph feature is broken for any project root deeper than '/'.","evidence":"let file_uid = format!(\"{project_id}::file::{}\", file_path.trim_start_matches('/'));","suggestion":"Reconstruct rel by stripping the project root (store root_path on the project row and strip_prefix it), or persist the rel path in a column and build the UID from that."},
  {"kind":"bug","severity":"medium","title":"Deduplicate chunk UIDs: start_line-only key overwrites same-line chunks","file":"src-tauri/src/ingest.rs","lines":"1017-1029","description":"Chunk UID is '{project_id}::{rel}::{start_line}' — start_line only. Any two definitions captured on the same starting line (minified JS with dozens of functions on line 1, one-line Rust impl blocks) produce colliding UIDs. INSERT OR REPLACE silently keeps only the last one — earlier definitions vanish from index and recall.","evidence":"let uid = format!(\"{project_id}::{}::{}\", pf.rel, chunk.start_line);","suggestion":"Include end_line and a disambiguating counter in the UID; reject same-start duplicates in collect_chunks."},
  {"kind":"bug","severity":"medium","title":"Do not delete good index data on transient per-file errors","file":"src-tauri/src/ingest.rs","lines":"306-312,455-469","description":"Any per-file failure (unreadable: EACCES/EBUSY while an editor holds the file, or temporarily non-UTF-8 content) pushes the rel into parse_error_rels, which deletes the indexed_files row AND adds existing chunk UIDs to stale_uids so vectors are permanently removed on replay. A single transient sharing-violation during a watch-triggered ingest wipes that file's chunks until the next event re-indexes it; recall silently loses the file.","evidence":"if let Some(e) = &pf.error {\n    result.errors.push(e.clone());\n    parse_error_rels.push(pf.rel.clone());\n    stale_file_uids.push(pf.file_uid.clone());","suggestion":"On per-file error, leave existing indexed_files row and chunks untouched (only report in result.errors); reserve deletion for files that no longer exist on disk."},
  {"kind":"bug","severity":"medium","title":"Run watcher ingest off the tokio runtime (blocking call in async task)","file":"src-tauri/src/io.rs","lines":"253-266","description":"The notify callback spawns an async task that directly calls run_watch_ingest_blocking — a full project ingest (tree-sitter parse, ONNX embed_batch, vector replay) — on a tauri::async_runtime worker thread. This blocks that tokio worker for the whole ingest, stalling other async commands/IPC sharing the runtime. operations.rs deliberately uses dedicated std::thread for manual ingests; the watch path bypasses that.","evidence":"tauri::async_runtime::spawn(async move {\n    tokio::time::sleep(Duration::from_secs(2)).await;\n    let _ = crate::operations::run_watch_ingest_blocking(&state_clone, &pid, &root);","suggestion":"Use tauri::async_runtime::spawn_blocking (or spawn a named std::thread like operations::spawn_ingest)."},
  {"kind":"bug","severity":"medium","title":"Surface watcher setup failures instead of silently marking project watched","file":"src-tauri/src/io.rs","lines":"273-281,196-203","description":"enable_watch writes watch_enabled=1 first, then spawn_watcher swallows both failure modes: recommended_watcher creation error → 'Err(_) => return', and watcher.watch(...) error → 'let _ ='. Project persisted as watched but nothing watches; resume_watches re-spawns a failing watcher every launch; no error reaches the user.","evidence":"Ok(w) => w,\nErr(_) => return,\n};\nlet _ = watcher.watch(root, notify::RecursiveMode::Recursive);","suggestion":"Make spawn_watcher return BiResult<()>; propagate watch() errors; on failure revert watch_enabled or report."},
  {"kind":"bug","severity":"medium","title":"Guard against concurrent ingests of the same project","file":"src-tauri/src/ingest.rs","lines":"355-368,455-500","description":"Nothing serializes ingests per project: a watch-triggered ingest and a manual/multi ingest can run simultaneously (operation lock is per-operation-id, not per-project). Both compute stale_uids/changed sets from their own DB snapshots (TOCTOU), and code_edges has no UNIQUE(project_id, from_uid, to_uid, edge_type) constraint, so interleaved delete+insert waves leave duplicate edge rows that accumulate across runs and inflate the graph.","evidence":"let conn = state.db.conn()?;\nlet mut stale_uids = Vec::new();\nfor rel in changed_rels.iter().chain(parse_error_rels.iter()) { ... }","suggestion":"Add a per-project ingest mutex/guard (project-scoped file lock like persistence's mutation lock), and add a UNIQUE index on code_edges(project_id, from_uid, to_uid, edge_type) with INSERT OR IGNORE."},
  {"kind":"bug","severity":"medium","title":"Fix '../' import resolution: trim_start_matches('.') collapses parent traversal","file":"src-tauri/src/ingest.rs","lines":"1424-1444","description":"For relative imports: candidate = base.join(import.trim_start_matches('.').trim_start_matches('/')). trim_start_matches('.') strips ALL leading dots, so '../utils/helper.ts' becomes 'utils/helper.ts' and resolves to <importing-dir>/utils/helper.ts instead of <parent>/utils/helper.ts. Every parent-relative import produces a wrong or missing edge; '../../x' collapses the same way. Only './' imports resolve correctly.","evidence":"let base = if import.starts_with('/') { root.to_path_buf() } else { from_file.parent()?.to_path_buf() };\nlet candidate = base.join(import.trim_start_matches('.').trim_start_matches('/'));","suggestion":"Count leading '../' segments, pop that many components from from_file.parent(), then join the remainder."},
  {"kind":"bug","severity":"medium","title":"Restrict JS/TS import query to require/import (no #eq? predicate)","file":"src-tauri/src/ingest.rs","lines":"1288-1293","description":"The js/ts import query captures the string argument of ANY call_expression with an identifier callee — unlike the Lua query which has (#eq? @fn \"require\"). Test/assertion calls like it(\"renders\"), describe(\"suite\"), fetch(\"/api\") all land in imports. Consequences: imports_json fills with garbage; resolve_import runs ~17 Path::exists() syscalls per bogus import; the by_basename fallback turns any first segment matching an existing file stem into a FALSE imports edge.","evidence":"(call_expression function: (identifier) @fn arguments: (arguments (string) @imp))","suggestion":"Add (#eq? @fn \"require\") and limit to import/export statements; skip resolving imports that don't look like paths/modules."},
  {"kind":"bug","severity":"medium","title":"Cap ingest file size and stream parsing to bound memory","file":"src-tauri/src/ingest.rs","lines":"969-975,258-277","description":"parse_one_file does read_to_end with no size limit before any check, and the walk never sets WalkBuilder::max_filesize. Combined with hidden(false) this means every .git packfile (hundreds of MB) is fully buffered then rejected as non-UTF-8, and a large generated dump is fully read + tree-sitter-parsed (10-30x expansion) → multi-GB spikes/OOM risk. The entire parsed: Vec<ParsedFile> is materialized before the single write transaction, so peak memory scales with total changed content.","evidence":"let mut bytes = Vec::new();\nif std::fs::File::open(path).and_then(|mut file| file.read_to_end(&mut bytes)).is_err() {","suggestion":"Set WalkBuilder::max_filesize (e.g. 1-2 MB) and/or early-continue via metadata().len(); flush ParsedFile batches through the DB write instead of accumulating all results."},
  {"kind":"bug","severity":"medium","title":"Deduplicate import_folder: re-importing a folder duplicates every memory","file":"src-tauri/src/io.rs","lines":"50-81","description":"remember() unconditionally creates a new memory per chunk; import_folder has no hash/UID dedup, so importing the same folder twice doubles all memories (and re-embeds everything). Also files_imported += 1 fires even when every chunk errored, and unreadable/non-UTF-8 files are silently skipped with no record in result.errors.","evidence":"match crate::memory::remember(state, input) {\n    Ok(_) => result.memories_created += 1,\n    Err(e) => result.errors.push(format!(\"{rel} chunk {i}: {e}\")),\n}\nresult.files_imported += 1;","suggestion":"Key imported memories by (file_path, chunk index, content hash) and upsert/skip existing; only count files_imported when >=1 chunk succeeded; push read failures into result.errors."},
  {"kind":"bug","severity":"medium","title":"Make .biturboignore creation non-fatal and race-safe","file":"src-tauri/src/ingest.rs","lines":"101-131","description":"ensure_biturboignore hard-fails the ENTIRE ingest if .gitignore exists but can't be read, or if .biturboignore can't be written — ingesting a read-only checkout/mount is impossible even though the ignore file is optional. TOCTOU: two concurrent first-ingests can both pass exists() and write concurrently. It also mutates the user's repository working tree as a side effect of indexing.","evidence":"let gitignore_content = std::fs::read_to_string(&gitignore)\n    .map_err(|e| BiError::Ingest(format!(\"failed to read {}: {e}\", gitignore.display())))?;","suggestion":"Log-and-continue on ensure_biturboignore errors instead of '?'; write via temp file + atomic rename, or skip creation when root isn't writable."},
  {"kind":"improvement","severity":"medium","title":"Add path filtering and cooldown to file watcher events","file":"src-tauri/src/io.rs","lines":"226-266","description":"Every Create/Modify/Remove event anywhere under root triggers ingest scheduling with only a 2s sleep + running/queued flag. During a build or git checkout (hundreds of events over minutes) the queued flag ping-pongs: each ingest finishes, sees queued=true, immediately starts another full project ingest — continuous back-to-back re-ingestion and CPU/ONNX burn. Events not filtered against ignore rules at all.","evidence":"state.running = false;\nif state.queued {\n    state.queued = false;\n    state.running = true;","suggestion":"Filter event paths through ignore logic (skip .git, gitignored paths), add minimum interval between watch ingests or proper debounce window."},
  {"kind":"bug","severity":"low","title":"Replay index mutations after commit even when cancelled","file":"src-tauri/src/ingest.rs","lines":"557-577","description":"check_cancelled sits between the committed DB write and replay_index_mutations. If cancel is requested during the write, ingest returns Err after the transaction committed: SQLite updated but vector journal not replayed, so turbovec index stays stale for the rest of the session (startup replay only heals on restart), and operation reported cancelled despite durable work.","evidence":"})?;\n\ncheck_cancelled(state, operation_id)?;\n...\nstate.replay_index_mutations(project_id)?;","suggestion":"Move cancellation check before the DB write only; always replay after successful commit (idempotent and cheap when journal empty)."},
  {"kind":"bug","severity":"low","title":"Report walk errors instead of silently skipping unreadable directories","file":"src-tauri/src/ingest.rs","lines":"218-222","description":"WalkBuilder results flattened with filter_map(|r| r.ok()), discarding per-entry errors (permission-denied subdirectories). User gets 'successful' ingest with silently partial index and empty errors list. A single chmod 000 directory makes an entire subtree invisible to memory with no signal. Same pattern in io.rs import_folder line 34.","evidence":".build()\n.filter_map(|r| r.ok())\n.filter(|e| e.path().is_file())","suggestion":"Collect walk errors (throttled) into result.errors or a warning count."},
  {"kind":"bug","severity":"low","title":"Handle uppercase extensions in detect_language","file":"src-tauri/src/ingest.rs","lines":"834-880","description":"Extension match is case-sensitive: 'MAIN.RS', 'UTIL.PY', 'App.TS' are not indexed. On macOS/Windows (case-insensitive filesystems) uppercase extensions are common, and the code already special-cases \"r\"|\"R\" showing awareness, but no other extension. Those files silently drop out of memory.","evidence":"let ext = p.extension()?.to_str()?;\nSome(match ext {\n    \"rs\" => \"rust\",","suggestion":"Normalize with ext.to_ascii_lowercase() before matching (keep R/r distinction by checking original)."},
  {"kind":"bug","severity":"low","title":"Index full HTML files instead of first 200 lines only","file":"src-tauri/src/ingest.rs","lines":"1262-1273","description":"HTML has no chunk query (chunk_query_src returns None), so collect_chunks falls back to a single chunk of at most the first 200 lines. For any real HTML file the remaining markup/inline scripts are silently unindexed, and start/end lines misrepresent coverage. The lang_bundles test asserts chunk_query.is_some() for all languages EXCEPT html, cementing the gap.","evidence":"if chunks.is_empty() {\n    let cap = (lines.len() - 1).min(200);\n    chunks.push(Chunk { code: lines[..=cap].join(\"\\n\"), ... });","suggestion":"Add an html chunk query, or window large fallback files into multiple 200-line chunks."},
  {"kind":"improvement","severity":"low","title":"Log query-compilation failures in LANG_BUNDLES","file":"src-tauri/src/ingest.rs","lines":"903-911","description":"chunk_query/import_query compilation failures collapsed via .ok() with no logging; a grammar/query version mismatch would silently degrade that language to whole-file fallback chunks with zero imports — diagnosable only in tests, not at runtime.","evidence":"let chunk_query = chunk_query_src(name).and_then(|src| Query::new(&language, src).ok());","suggestion":"Log tracing::warn! with the query error per language on failure."},
  {"kind":"bug","severity":"low","title":"Skip or resolve symlinked files explicitly in ingest walk","file":"src-tauri/src/ingest.rs","lines":"220-221","description":"follow_links(false) prevents directory recursion, but yielded symlink file entries pass e.path().is_file(), which FOLLOWS the link. A symlink inside the repo pointing outside it gets its target content read, hashed, and indexed under the in-repo path — content from outside the project root enters memory; same target reachable via two links indexed twice under different rels. Unvetted trust-boundary crossing for a local memory layer.","evidence":".filter(|e| e.path().is_file())","suggestion":"Use symlink_metadata() to skip entries where file_type().is_symlink(), or dedupe by canonical path."},
  {"kind":"bug","severity":"low","title":"Guard collect_chunks slicing against zero-width EOF nodes","file":"src-tauri/src/ingest.rs","lines":"1236-1255","description":"end clamped to lines.len()-1 but start not validated. A zero-width node at EOF (error-recovery MISSING tokens sit at row == lines.len() when file ends with newline) would make start > lines.len()-1 and end < start, so lines[start..=end] panics; running inside rayon PARSE_POOL this aborts the entire ingest with panic instead of ingest error.","evidence":"let end = end_node.end_position().row.min(start + 200).min(lines.len() - 1);\nlet code = lines[start..=end].join(\"\\n\");","suggestion":"Clamp start to lines.len()-1 and skip capture if start > end."},
  {"kind":"bug","severity":"low","title":"Extract PHP use-statements: capture kind never matched in collect_imports","file":"src-tauri/src/ingest.rs","lines":"1310-1314,1384-1407","description":"PHP import query captures (namespace_use_declaration) @imp, but collect_imports only handles string/string_literal/interpreted_string_literal, import_declaration/import_header, identifier/dotted_name/scoped_identifier. 'namespace_use_declaration' matches none — every PHP capture silently dropped; PHP files always get zero imports and no import edges while the query pretends support.","evidence":"\"php\" => { r#\" (namespace_use_declaration) @imp \"# }","suggestion":"Add branch for namespace_use_declaration stripping leading 'use ' and trailing ';', or capture inner qualified names."},
  {"kind":"bug","severity":"low","title":"Preserve chunk stats: INSERT OR REPLACE resets created_at/access_count","file":"src-tauri/src/ingest.rs","lines":"789-830","description":"Re-indexing a changed file replaces chunk rows with hardcoded created_at=now, last_access=now, access_count=0, importance=0.5. Usage statistics accumulated by recall (which feed ranking/consolidation) wiped every time a file is touched; created_at no longer reflects when chunk first appeared.","evidence":"INSERT OR REPLACE INTO memories (uid, project_id, mem_type, content, importance, created_at, updated_at, last_access, access_count, ...)","suggestion":"Use INSERT ... ON CONFLICT(uid) DO UPDATE SET content=..., updated_at=... leaving created_at/access_count/last_access intact since UIDs deterministic."},
  {"kind":"improvement","severity":"low","title":"Bump an indexer version into the file hash to invalidate stale chunkings","file":"src-tauri/src/ingest.rs","lines":"320-322","description":"Changed-file detection compares only file-content SHA-256. If chunker changes (grammar versions, query tweaks, MAX_CHUNK_TEXT changes), unchanged files keep old chunks forever — old segmentation, old UIDs, old imports — no way to refresh short of editing every file or deleting DB.","evidence":"let unchanged = existing_files.get(&pf.rel).is_some_and(|existing| existing.file_hash == pf.file_hash);","suggestion":"Hash file_hash together with an INDEXER_VERSION constant into the comparison key stored in indexed_files."},
  {"kind":"bug","severity":"low","title":"Fix chunk content truncation mismatch with advertised line span","file":"src-tauri/src/ingest.rs","lines":"127-134","description":"db_content() truncates to 4000 chars, but a 200-line chunk of dense code can exceed that; the row still advertises full start_line..end_line span. Recall/MCP returns code cut mid-expression while claiming to cover e.g. L120-320, and get_project_graph sizes nodes from full span. Silent content loss for exactly the large functions users most want.","evidence":"if text.len() > MAX_CHUNK_TEXT {\n    text = text.chars().take(MAX_CHUNK_TEXT).collect();\n}","suggestion":"Store full content, or clamp advertised end_line to truncation boundary so consumers know true extent."},
  {"kind":"improvement","severity":"low","title":"Batch stale-chunk UID lookups instead of per-file queries","file":"src-tauri/src/ingest.rs","lines":"355-368","description":"stale_uids built with one code_uids_for_file query per changed/errored/deleted file — on a large refactor (thousands of changed files) that is thousands of sequential prepared-statement executions on the hot path before the write transaction.","evidence":"for rel in changed_rels.iter().chain(parse_error_rels.iter()) {\n    stale_uids.extend(db::code_uids_for_file(&conn, project_id, rel)?);\n}","suggestion":"Add db::code_uids_for_files using WHERE file_path IN (...) chunked batches, mirroring delete_memories_by_uids."},
  {"kind":"bug","severity":"low","title":"Align build_structure_summary filters with the ingest walk","file":"src-tauri/src/ingest.rs","lines":"1487-1497","description":"Structure summary omits add_custom_ignore_filename(\".biturboignore\") and uses hidden(true) while ingest walk uses hidden(false) and honors .biturboignore. Embedded project-structure summary lists ignored generated dirs inconsistently with what is actually indexed.","evidence":"WalkBuilder::new(root)\n    .max_depth(Some(3))\n    ...\n    .hidden(true)","suggestion":"Add .add_custom_ignore_filename(\".biturboignore\") and use same hidden setting as ingest walk (plus .git exclusion fix)."},
  {"kind":"improvement","severity":"low","title":"Stream memory export instead of buffering entire JSON in RAM","file":"src-tauri/src/io.rs","lines":"138-149","description":"export_memories loads up to 1,000,000 memories via list() then serde_json::to_string_pretty over the whole set — two full copies of export in memory, easily GBs for large project. create_dir_all(parent).ok() swallows directory-creation errors, deferring to confusing write failure.","evidence":"let mems = crate::memory::list(state, project_id, None, 1_000_000, 0)?;\nlet json = serde_json::to_string_pretty(...)?;","suggestion":"Write with serde_json::Serializer over a File and stream memories in pages; propagate create_dir_all errors."},
  {"kind":"bug","severity":"low","title":"Log first watch-ingest failure instead of discarding the Result","file":"src-tauri/src/io.rs","lines":"255-256","description":"First (non-queued) watch ingest's Result discarded with 'let _ =' while only queued retry path logs errors. If every watch ingest fails same way, nothing ever logged — watch mode appears healthy while silently doing nothing.","evidence":"let _ = crate::operations::run_watch_ingest_blocking(&state_clone, &pid, &root);","suggestion":"Match on Result and tracing::error! the failure, same as queued branch."},
  {"kind":"improvement","severity":"low","title":"Remove dead code: _unused_hashset, unused file_name, _kind binding","file":"src-tauri/src/ingest.rs","lines":"1031,1256,1552-1555","description":"Leftover cruft: #[allow(dead_code)] fn _unused_hashset() existing only to silence unused-import warning (HashSet IS used elsewhere), 'let _ = file_name;' computed never used, '_kind' binding computed and discarded per chunk.","evidence":"#[allow(dead_code)]\nfn _unused_hashset() -> HashSet<()> { HashSet::new() }","suggestion":"Delete _unused_hashset and _kind binding; remove file_name computation or use it."}
]
